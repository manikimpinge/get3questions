- if flash.present?
  - if flash[:notice]
    .alert.alert-success
      %button.close{"data-dismiss" => "alert"} x
      = flash[:notice].html_safe
  - if flash[:alert]
    .alert.alert-error
      %button.close{"data-dismiss" => "alert"} x
      = flash[:alert].html_safe
